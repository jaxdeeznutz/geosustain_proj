
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/geosustain_db

SECRET_KEY=change-me-to-a-long-random-secret

GEE_PROJECT=capstone-493314

# Firebase project id — used to cryptographically verify Firebase Auth ID
# tokens server-side (e.g. when completing Firebase email verification).
# Defaults to the project's own Firebase project id if unset.
FIREBASE_PROJECT_ID=geosustain-4c722


